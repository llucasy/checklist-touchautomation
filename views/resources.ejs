<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Recursos</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" type="text/css" href="/stylesheets/resources.css">
</head>
<body>
  <%- include('admin/header'); %>
  <main>
    <p>Recursos cadastrados: <%= count %></p>
    <button id="myBtn">Cadastrar novo recurso</button>
    <div id="myModal" class="modal">
      <div class="modal-content">
        <span class="close">&times;</span>
        <p>Digite os dados abaixo:</p>
        <form action="/resources" method="post">
          <label for="recurso">Recurso:</label>
          <input type="text" id="recurso" name="category" required>
          <label for="categoria">Categoria:</label>
          <input type="text" id="categoria" name="subCategories" required>
          <input type="button" value="Adicionar categoria">
          <input type="submit" value="Cadastrar">
        </form>
      </div>
    </div>
    <section>
      <% if(!docs || docs.length === 0) { %>
      <a>Não há recrusos cadastrados</a>
      <% } else { docs.forEach((resource) => { %> 
      <a href="/changeresource/<%= resource._id %>"><b>Recurso:</b> <%= resource.category %></a>
      <% }) } %>
    </section>
  </main>
  <footer>
    <p><a href="/">Sair</a></p>
  </footer>
  <script src="/javascript/resources.js"></script>
</body>
</html>